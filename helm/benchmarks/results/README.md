# Benchmark results

Run artifacts land in `<run_id>/` subdirectories:

- `meta.json` — orchestrator metadata
- `report.json` — collector output

Comparison reports: `compare-<baseline>-vs-<helm>/REPORT.md`

Manual dry-run notes from Task 16 can be appended below.

---

## Dry run log

_(Operator: record HELM_ENABLED=0/1 runs, pass/fail, and links to compare reports.)_
